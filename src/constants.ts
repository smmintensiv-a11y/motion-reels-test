// Source: оксана.MOV — 720x1280, 30fps, 7.6s (228 frames), h264/aac with audio.
// Already native 9:16, so it fills 1080x1920 with a plain scale — no crop needed.
export const VIDEO_FPS = 30;
export const VIDEO_WIDTH = 1080;
export const VIDEO_HEIGHT = 1920;
export const VIDEO_DURATION_FRAMES = 228;

export const ACCENT_COLOR = '#D7FF3E';
